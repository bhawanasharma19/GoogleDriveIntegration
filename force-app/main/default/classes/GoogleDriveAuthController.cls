public with sharing class GoogleDriveAuthController {
    /*@AuraEnabled(cacheable=true)
    public static String generateAuthUrl() {
        return GoogleDriveAuthCode.createAuthURL();
    }
    
    @AuraEnabled
    public static Boolean getAccessToken(String code) {
        return GoogleDriveAuthCode.getAccessToken(code);
    }*/
}